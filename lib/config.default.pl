#!/usr/bin/perl
#
# Configuration variables used by the database routines
#

use strict;

# Define globals for database access
$main::database_user_id = "openid";
$main::database_password = "s3cr3t";
$main::database_name = "openid";
$main::database_server = "localhost";

1;
